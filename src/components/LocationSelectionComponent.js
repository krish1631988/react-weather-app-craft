import React, { Component } from 'react';

class LocationSelectionComponent extends Component {
  render() {
    return (
      <div>
        <p>LocationSelectionComponent to hold the widget to select location.</p>
      </div>
    );
  }
}

export default LocationSelectionComponent;
